{
  "name": "peeky-demo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev-server": "nodemon ../peeky-server/dist/standalone.js --watch .. --delay 200ms -e js --ignore tests/ --ignore demo/",
    "dev-open": "peeky open",
    "dev-run": "peeky run"
  },
  "devDependencies": {
    "@peeky/cli": "^0.1.0",
    "@peeky/runner": "^0.1.0",
    "nodemon": "^2.0.7",
    "ts-node": "^9.1.1"
  }
}
